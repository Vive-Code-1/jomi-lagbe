

## ডেমো জমি ফিক্স ও এডমিন থেকে ম্যানেজমেন্ট

### সমস্যা
1. হোমপেজে ডেমো জমির কার্ড দেখাচ্ছে কিন্তু ছবি লোড হচ্ছে না — Unsplash URL ব্লক/slow হতে পারে
2. গার্বেজ টেস্ট ডাটা আছে (`2312sdasd` নামের একটি জমি) যেটা ক্লিন করা দরকার
3. এডমিন ড্যাশবোর্ডে সব জমি ইতোমধ্যে দেখা ও এডিট/ডিলিট করা যায় (CRUD কাজ করছে)

### সমাধান

**১. গার্বেজ টেস্ট ডাটা ক্লিন:**
- `4f108a34` ID এর টেস্ট জমি (`2312sdasd`) ডিলিট করা হবে

**২. ডেমো জমির ছবি আপডেট:**
- সব ডেমো জমির images Unsplash থেকে আরো reliable ও diverse ছবি দিয়ে আপডেট করা হবে (প্রতিটি জমির জন্য আলাদা ছবি)
- বর্তমানে অনেকগুলোতে একই ছবি (`photo-1500382017468`) ব্যবহার হচ্ছে — এটি ভিন্ন ভিন্ন করা হবে

**৩. এডমিন ড্যাশবোর্ড ভেরিফাই:**
- এডমিন ড্যাশবোর্ডের Listings সেকশনে সব ১০টি ডেমো জমি দেখা যাচ্ছে এবং Edit/Delete কাজ করছে — এটি ইতোমধ্যে কাজ করে (admin RLS policy আছে)

### পরিবর্তন
1. **Database update** — গার্বেজ ডাটা ডিলিট + ডেমো জমির ইমেজ আপডেট (diverse, reliable URLs)

